import React from 'react';

const initState = {};
export const appReducer = (state: any = initState, action: any): any => {

    switch (action.type) {

        default:
            return state
    }
}

